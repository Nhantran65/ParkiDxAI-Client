/**
 * @deprecated Will be removed later because it violates the principles of RESTful API, use subcode instead
 */
export enum JFWHttpCode {
    SUCCESS = 200,
    NOT_PERMISSION = 209,
    BAD_REQUEST = 400,
    NOT_AUTHORIZE = 403,
    NOT_FOUND = 404,
    MAX_DEVICE = 405,
    WRONG_PASSWORD = 701,
    USER_NOT_FOUND = 704,
    EMAIL_EXISTS = 723,
}
