export interface IFallbackLoadingProps {
    /**
     * Whether to show loading in fullscreen mode.
     * @default false
     */
    isFullscreen?: boolean;
    
    /**
     * Whether to center the loading spinner.
     * @default false
     */
    isCenter?: boolean;
}
