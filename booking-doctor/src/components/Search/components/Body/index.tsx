import { SearchBodyWrapper } from './styles';

export const SearchBody = () => {
    return (
        <SearchBodyWrapper>
            {/* <Input.Search placeholder="Search for user by email address or username" /> */}
        </SearchBodyWrapper>
    );
};
