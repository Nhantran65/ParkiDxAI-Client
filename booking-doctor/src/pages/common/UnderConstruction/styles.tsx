import { Layout } from '1byte-react-design';
import Page from '@/components/Page';
import styled from '@emotion/styled';

export const UnderConstructionLayout = styled(Layout)`
    justify-content: center;
    align-items: center;
    min-height: 100vh;
`;

export const UnderConstructionWrapper = styled(Page)``;
