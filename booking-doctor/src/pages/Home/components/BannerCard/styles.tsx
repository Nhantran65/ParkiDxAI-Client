import { Card } from '1byte-react-design';
import styled from '@emotion/styled';

export const BannerCardWrapper = styled(Card)`
    width: 786px;
    position: absolute;
    bottom: -100px;
    left: 50%;
    transform: translateX(-50%);
    background-color: #fff;
`;
