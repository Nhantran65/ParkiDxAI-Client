import { Popover } from '1byte-react-design';
import styled from '@emotion/styled';

export const HeaderNotificationWrapper = styled(Popover)``;

export const HeaderNotificationPopoverWrapper = styled.div``;
